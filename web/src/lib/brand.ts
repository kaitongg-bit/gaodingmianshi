export function brandName(locale: string): string {
  return locale === "zh" ? "稿定面试" : "DraftReady";
}

export function brandSubtitle(locale: string): string {
  return locale === "zh" ? "InterviewScript" : "InterviewScript";
}
